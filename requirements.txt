# ── Core framework ────────────────────────────────────────────────────────────
langgraph>=0.2
langchain>=0.2
langchain-community>=0.2
pydantic>=2.0
pydantic-settings>=2.0

# ── LLM providers ────────────────────────────────────────────────────────────
groq>=0.9
google-genai==2.10.0

# ── Memory / cache (Redis only — no vector store) ────────────────────────────
redis>=5.0

# ── Web search & scraping ─────────────────────────────────────────────────────
ddgs==9.14.4
scholarly>=1.7
playwright>=1.44

# ── Academic / PDF ────────────────────────────────────────────────────────────
PyMuPDF>=1.24
unstructured>=0.14
# force numba/llvmlite versions that support Python 3.12
numba>=0.57
llvmlite>=0.40
arxiv>=2.1

# ── Observability / evaluation ────────────────────────────────────────────────
ragas>=0.1
langfuse>=2.0

# ── UI ───────────────────────────────────────────────────────────────────────
streamlit>=1.35

