# memory/ — Redis-only memory layer
# No vector store / Chroma / embeddings in this project.
# Provides:
#   session_store  — agent scratchpads and session state (short-term)
#   facts_store    — extracted facts persisted per research session
